# QA record: issue #484

- Date (UTC): `2026-07-20`
- Issue: [`#484`](https://github.com/BeFeast/ok-player/issues/484)
- Decision: `BLOCKED`
- Scope: Verify whether the hands-off Windows candidate acceptance cycle required
  by issue #484 can be observed against current `main`, and record the exact
  missing prerequisite. This record accepts nothing and rejects no candidate; it
  documents that the acceptance cannot run because the `win-candidate` delivery
  surface owned by issue #483 does not yet exist.

## Provenance

| Item | Exact identity |
| --- | --- |
| Repository base | `f1d5ad05f85fdf36b7c7ab09b799bbfe51a597fa` |
| Tested source | `f1d5ad05f85fdf36b7c7ab09b799bbfe51a597fa` |
| Candidate source | `N/A` — no promoted `win-candidate` exists to test |
| Candidate/build | `N/A` — the `windows-candidate` rolling prerelease and `releases.win-candidate.json` feed do not exist |
| Blocking dependency | [`#483`](https://github.com/BeFeast/ok-player/issues/483) — Windows candidate lane (`release-windows-candidate.yml`, `win-candidate` Velopack channel, rolling `windows-candidate` prerelease + feed) |
| Related pull request | Created from issue #484; number assigned after push |

## Environment

| Item | Value |
| --- | --- |
| Operating system | Linux x86_64 orientation host; repository and public GitHub endpoint inspection only |
| Desktop/session | Headless; no application launched (no candidate to install) |
| Package lane | `win-candidate` Velopack channel — not present on `main` |
| Hardware class | Persistent Windows 11 acceptance VM (issues #343/#399) intentionally not engaged; no candidate to install |
| Runtime versions | `N/A` — no installed candidate to report app, mpv, or Velopack versions from |

No hostname, account name, machine path, credential, private address, or
infrastructure identifier is retained in this record. The persistent Windows 11
VM and its access path are referenced abstractly only.

## Prerequisite verification

The acceptance sequence in #484 consumes the delivery contract defined by #483:
a promoted candidate to trigger on, an in-app Velopack update path to exercise,
and a rolling candidate manifest to write accepted/rejected evidence into. Each
prerequisite was checked against base `f1d5ad05f85fdf36b7c7ab09b799bbfe51a597fa`:

| Prerequisite | Expected when #483 has landed | Observed on `main` |
| --- | --- | --- |
| Issue #483 status | Closed with a merged PR | Open; no pull request references or implements it |
| Candidate build workflow | `.github/workflows/release-windows-candidate.yml` | Absent (`.github/workflows/` holds `ci`, `publish-update-feeds`, `release-linux-candidate`, `release-linux`, `rpm`, `rust` only) |
| `win-candidate` implementation | References under `.github/`, `scripts/`, `docs/` | None — no `win-candidate`/`windows-candidate` references in any of those trees |
| Rolling `windows-candidate` prerelease | Release tag resolves (HTTP 200), as `linux-candidate` does | `releases/tag/windows-candidate` → HTTP 404 (contrast: `releases/tag/linux-candidate` → HTTP 200) |
| Candidate feed endpoint | `releases.win-candidate.json` served | HTTP 404 — no promoted candidate to poll, trigger on, or accept |

The Windows VM path (`scripts/bootstrap-windows-dev-vm.ps1`) and the Linux
acceptance-manifest tooling that #484 must mirror in shape
(`scripts/write-linux-acceptance-template.sh`,
`scripts/merge-linux-acceptance-evidence.sh`,
`scripts/measure-linux-acceptance.sh`) are present. The only missing input is the
promoted Windows candidate and its feed/manifest surface from #483.

## Result matrix

| Check | Evidence level | Expected | Actual | Status | Full log |
| --- | --- | --- | --- | --- | --- |
| Trigger on new promoted candidate (exactly once per version) | workflow/feed | Poll `win-candidate` feed or subscribe to candidate workflow completion; fire once per candidate version | No `win-candidate` feed or candidate workflow exists to trigger on | BLOCKED | This record |
| In-app Velopack update applied in guest | installed Windows VM | Query updater state and migrate through the app's own Velopack path | No promoted candidate package exists to update to | BLOCKED | This record |
| Functional smoke: start, window render, media open, playback advance, seek, orderly shutdown | installed Windows VM | Smoke passes end to end | Cannot install a nonexistent candidate | NOT RUN | This record |
| Evidence bundle: app logs, versions, package SHA-256 | installed Windows VM | Collect and bind acceptance evidence | No candidate package or run to collect evidence from | NOT RUN | This record |
| Manifest records accepted/failed (same shape as Linux acceptance manifest) | manifest | Rolling candidate manifest carries accepted/rejected + evidence pointers | No rolling `win-candidate` manifest surface exists to write into | BLOCKED | This record |
| Failure path surfaces as a red check and holds the channel | orchestrator | Failed acceptance marks candidate rejected and blocks channel advance | No candidate, feed, or channel to reject or hold | NOT RUN | This record |
| One full hands-off cycle observed | end to end | Candidate promoted → VM self-updates → smoke passes → manifest records acceptance, no manual steps | No candidate can be promoted; the cycle cannot begin | BLOCKED | This record |

## Artifact checksums

| Artifact | SHA-256 | Durable location |
| --- | --- | --- |
| `N/A` — no package, feed, manifest, or evidence bundle produced | `N/A` | No promoted `win-candidate` exists |

## Limitations and holds

- This is a blocked-prerequisite record, not an acceptance run. No candidate was
  installed, no smoke was executed, and no evidence bundle was produced, because
  no promoted `win-candidate` exists.
- The block is a hard dependency on #483. #484 cannot be implemented or accepted
  without guessing the #483 feed/package/manifest contract, and the required
  full hands-off observation is impossible with no candidate in existence.
- Requeue #484 once #483 has landed and the first `win-candidate` is promoted
  to the rolling `windows-candidate` prerelease with a live
  `releases.win-candidate.json` feed. At that point this record's `BLOCKED` and
  `NOT RUN` rows become the executable acceptance matrix to satisfy.
- No live-desktop, GPU/D3D11, HDR, 4K60, or multi-monitor DPI claim is made or
  implied; those remain on physical hardware per the issue's non-goals.
